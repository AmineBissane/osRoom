eureka:
  instance:
    hostname: osroom-discovery-1
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://osroom-discovery-1:8761/eureka/
server:
  port: 8761
